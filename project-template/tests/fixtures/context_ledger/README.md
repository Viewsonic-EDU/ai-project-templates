Golden excerpt captured 2026-09-16 from real local Claude transcripts.
Usage (including nested iterations) and the synthetic error schema are retained.
Request identifiers, model identity and all content are scrubbed; all other metadata is omitted.
The newest transcript had no synthetic error; that record came from another recent transcript.
Compaction markers in tests are SYNTHETIC: no real marker has been observed.
